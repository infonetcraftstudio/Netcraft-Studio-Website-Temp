# NetCraft Studio Corporate UI

A responsive static corporate website concept for NetCraft Studio, aligned with the company's blue-violet geometric brand and its existing service, portfolio, and contact structure.

## Run

Open `index.html` directly in a browser, or serve the folder with any static server:

```bash
python -m http.server 4173
```

Then visit `http://localhost:4173`.
